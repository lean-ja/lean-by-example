/- # MacroM

`MacroM` は、マクロ展開のための主要な[モナド](#{root}/TypeClass/Monad.md)です。マクロ衛生を実現するために必要な状態を保持しています。
-/

/- ## 使用例

### オセロのゲーム盤

`Macro` 型の関数および `MacroM` モナドに包まれた関数を使用して、オセロのゲーム盤を表現する構文を定義することができます。

{{#include ./MacroM/OthelloBoard.md}}
-/

/- ### 指数表記マクロ

`a¹`、`a²`、`a³` のような指数表記をマクロで定義することもできます。以下の例では、`aⁿ` のような構文を定義しています。

{{#include ./MacroM/Exponent.md}}
-/

/- ### ラベル付き木の項表示

ラベル付き木に対する項表示の構文をマクロで定義することができます。

{{#include ./MacroM/TreeSyntax.md}}
-/
