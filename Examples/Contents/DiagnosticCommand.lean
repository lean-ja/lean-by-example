/- # 対話的コマンド
Lean のコマンドの中には `#` で始まるものがあります．これは対話的なコマンドで，実行結果をエディタ内で即座に確認できるようになっています．
-/

/-- アッカーマン関数 -/
def ack : Nat → Nat → Nat
  | 0, y => y+1
  | x+1, 0 => ack x 1
  | x+1, y+1 => ack x (ack (x+1) y)

-- 1 と表示される
#eval ack 0 0
